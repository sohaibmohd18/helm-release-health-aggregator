apiVersion: v1
kind: Service
metadata:
  name: helmsights
  namespace: helmsights
  labels:
    app.kubernetes.io/name: helmsights
    app.kubernetes.io/component: controller
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: helmsights
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
