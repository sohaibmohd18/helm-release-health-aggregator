apiVersion: v1
kind: Service
metadata:
  name: helmsight
  namespace: helmsight
  labels:
    app.kubernetes.io/name: helmsight
    app.kubernetes.io/component: controller
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: helmsight
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
